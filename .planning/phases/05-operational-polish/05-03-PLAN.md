# Plan 05-03: Empty States, Loading, & Error Handling

**Phase:** 05 — Operational Polish
**Wave:** 3 of 3
**Goal:** Close all UX gaps so the app feels complete on first launch and handles failures gracefully. No more "broken-looking" screens.

## Tasks

| # | Task | Key Files |
|---|------|-----------|
| 1 | Audit and fix empty states across all screens | Multiple component files |
| 2 | Add loading shimmer/skeleton to Entry and Party pages | `DailyEntryPage.tsx`, `PartyListPage.tsx` |
| 3 | Add error boundary with retry | `client/src/components/ErrorBoundary.tsx`, `App.tsx` |
| 4 | Add first-launch hint on Entry page | `DailyEntryPage.tsx` |
| 5 | Update STATE.md and ROADMAP.md | `.planning/STATE.md`, `.planning/ROADMAP.md` |

## Task Details

### Task 1: Empty state audit
Screens to check and fix:

| Screen | Current empty state | Fix needed |
|--------|-------------------|------------|
| Daily Entry (no data) | Shows empty revenue rows (OK — fields are always visible) | None — already handled |
| Dashboard KPIs (no data) | Shows €0.00 values | Add subtle "No data yet" hint below KPIs when all are 0 |
| Revenue Trend (no data) | "No entries yet for this month" | OK |
| Category Breakdown (no data) | "Enter some data to see breakdowns" | OK |
| Profit Detail (no data) | "No data for this period" | OK |
| Kids Trend (no data) | "No kids entries in the last 30 days" | OK |
| Party list (no parties) | "No parties this month" | OK |
| Party widget (no completed) | "No completed parties this month" | OK |
| Dashboard Parties filter (no data) | KPIs show 0, charts hidden | Add hint: "No party data yet — mark parties as completed to see revenue" |

Most empty states are already handled. Focus on the 2 gaps above.

### Task 2: Loading states
- Entry page: show subtle pulse/shimmer on revenue section while data loads (currently just opacity-50)
- Party list: show 2-3 skeleton cards while loading
- Keep it minimal — the opacity-50 transition already works, just needs a slight upgrade for perceived speed

### Task 3: Error boundary
- Create `ErrorBoundary` class component wrapping the app
- Shows: "Something went wrong" + "Try again" button (calls `window.location.reload()`)
- Styled in the dark theme — not a jarring white error page
- Wrap `<App />` in `main.tsx`

### Task 4: First-launch hint
- When Entry page loads with all revenues empty AND no lastSaved timestamp
- Show a subtle card: "Start by entering today's numbers — revenue fields are grouped by business area"
- Dismisses automatically once any field has a value
- Light, non-intrusive — just a nudge

### Task 5: Update planning docs
- Mark Phases 3 and 4 as complete in STATE.md (currently only 1-2 are marked)
- Mark Phase 5 as complete
- Update ROADMAP.md phase checkboxes
- Add recent activity entries

## Verify

```bash
npx tsc --noEmit -p client/tsconfig.json
# Manual: fresh database, verify all screens show intentional empty states
# Manual: kill server, verify error boundary catches fetch failures
```
